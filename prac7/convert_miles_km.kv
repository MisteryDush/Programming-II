BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'horizontal'
        TextInput:
            id: input
            text:'34'
            on_text: app.handle_conversion()
        BoxLayout:
            orientation: 'vertical'
            Button:
                text: 'Up'
                on_press: app.increment_one(1)
            Button:
                text: 'Down'
                on_press: app.increment_one(-1)

    #Button:
        #text: 'Convert miles to km'
        #on_press: app.handle_conversion()

    Label:
        text: 'Output here'
        id: result
